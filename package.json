
{
  "name": "react-native-ssh",
  "version": "1.0.1",
  "description": "a library that enables React Native apps to execute commands over SSH",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "react-native",
    "ssh"
  ],
  "author": "",
  "license": "",
  "peerDependencies": {
    "react-native": "^0.40.0"
  }
}
